import { useController } from "react-hook-form"

export const TextInputField = ({control, type="text", name, defaultValue='', errMsg=null}) => {
    const {field} = useController({
        

    })
    return(<>
    
    </>)
}